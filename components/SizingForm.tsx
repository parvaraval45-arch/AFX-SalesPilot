"use client";

// This component is deprecated — the AFX Sizer page now handles sizing directly.
// Kept as a placeholder for potential reuse in other contexts.

export default function SizingForm() {
  return (
    <div className="text-center text-netapp-muted py-12">
      <p className="text-sm">
        Sizing functionality has been integrated into the AFX Sizer page.
      </p>
    </div>
  );
}
